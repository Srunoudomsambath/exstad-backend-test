spring.application.name=exstad-backend-api
spring.datasource.url=jdbc:postgresql://dpg-d729fs450q8c738llbrg-a.oregon-postgres.render.com:5432/exstad_backend
spring.datasource.username=exstad_backend_user
spring.datasource.password=ZeSBfnsuL4BCRg6ekntnOFYzNOKj4Zzb
spring.datasource.driver-class-name=org.postgresql.Driver
#spring.datasource.url=jdbc:postgresql://${DB_HOST}:${DB_PORT}/${DB_NAME}
#spring.datasource.username=${DB_USERNAME}
#spring.datasource.password=${DB_PASSWORD}

spring.jpa.hibernate.ddl-auto=update
spring.jpa.generate-ddl=true
#spring.jpa.show-sql=true
#spring.jpa.properties.hibernate.format_sql=true

keycloak.server-url=${KC_SERVER_URL}
keycloak.realm=${KC_REALM}
keycloak.client-id=${KC_CLIENT_ID}
keycloak.secret-id=${KC_SECRET_ID}

keycloak.frontend-client-id=exstad-frontend
keycloak.frontend-secret-id=f0cS9T3mOKfmBQdrGbbeblygW88oEitc
spring.security.oauth2.resourceserver.jwt.issuer-uri=${KC_SERVER_URL}/realms/${KC_REALM}

spring.servlet.multipart.max-request-size=500MB
spring.servlet.multipart.max-file-size=500MB
# ... all your existing settings ...

# ADD THESE NEW LINES:
# Tomcat settings for large uploads
server.tomcat.max-swallow-size=-1
server.tomcat.max-http-form-post-size=-1
server.tomcat.connection-timeout=3600000

# Multipart additional settings
spring.servlet.multipart.enabled=true
spring.servlet.multipart.file-size-threshold=0

minio.endpoint=${MINIO_ENDPOINT}
minio.access-key=${MINIO_ACCESS_KEY}
minio.secret-key=${MINIO_SECRET_KEY}
minio.bucket=exstad
server.public-access=${SERVER_PUBLIC_ACCESS}

# BAKONG
bakong.account-id=${BAKONG_ACCOUNT_ID}
bakong.acquiring-bank=${ACQUIRINGBANK}
bakong.merchant-name=${MERCHANTNAME}
bakong.mobile-number=${MOBILENUMBERSTORELABEL}
bakong.store-label=${STORELABEL}
bakong.base-url=${BAKONG_BASE_URL}
bakong.bearer-token=${BAKONG_BEARER_TOKEN}